pipeline {
    agent {
        node { label 'windows' }
    }
    stages {
        stage('Print_Hello_World') {
            steps {
                echo 'Hello world!'
            }
        }
    }
}
